rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /matches/{matchId} {
      allow read, write: if request.auth != null;
    }

    match /word-rush-matches/{matchId} {
      allow read, write: if request.auth != null;
    }
  }
}
